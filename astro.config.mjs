import { defineConfig } from 'astro/config';

// Practice site config — swap `site` for the real domain when this
// gets deployed for real.
export default defineConfig({
  site: 'https://example.com',
});
